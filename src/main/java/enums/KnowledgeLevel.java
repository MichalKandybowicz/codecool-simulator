package enums;

public enum KnowledgeLevel {
    Beginner(1),
    PRE_INTERMEDIATE(2),
    INTERMEDIATE(3),
    UPPER_INTERMEDIATE(4),
    ADVANCED(5),
    PROFICIENT(6),
    MASTER(7);

    KnowledgeLevel(int level) {
    }
}
